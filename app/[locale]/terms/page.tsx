import { getCopy } from "@/lib/content";
import { isLocale } from "@/lib/i18n";
import { notFound } from "next/navigation";
export default async function TermsPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const copy = getCopy(locale); return <article className="shell max-w-3xl py-14"><h1 className="text-4xl font-bold">{copy.legal.terms}</h1><p className="mt-5 leading-7 text-slate-600">NavGrove provides curated links and short descriptions for discovery. The information is provided as-is and may change without notice.</p><h2 className="mt-10 text-xl font-semibold">External services</h2><p className="mt-3 leading-7 text-slate-600">When you visit a listed product, you leave NavGrove and are subject to that product’s terms, pricing and privacy policy. NavGrove does not guarantee the availability or performance of external services.</p><h2 className="mt-10 text-xl font-semibold">Corrections</h2><p className="mt-3 leading-7 text-slate-600">To suggest a correction or submit a product, contact <a className="text-green-700" href="mailto:support@navgrove.com">support@navgrove.com</a>.</p></article>; }
