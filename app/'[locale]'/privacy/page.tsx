import { getCopy } from "@/lib/content";
import { isLocale } from "@/lib/i18n";
import { notFound } from "next/navigation";
export default async function PrivacyPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const copy = getCopy(locale); return <article className="shell max-w-3xl py-14"><h1 className="text-4xl font-bold">{copy.legal.privacy}</h1><p className="mt-5 text-slate-600">NavGrove is a static discovery website operated through navgrove.com. We do not require accounts, store search history, or run advertising trackers in this version.</p><h2 className="mt-10 text-xl font-semibold">Information you choose to send</h2><p className="mt-3 leading-7 text-slate-600">When you use the submission link, your email application sends the information directly to support@navgrove.com. NavGrove does not receive it until you choose to send the message.</p><h2 className="mt-10 text-xl font-semibold">External websites</h2><p className="mt-3 leading-7 text-slate-600">Links to third-party websites are governed by their own privacy policies. Search requests sent to Google or Baidu are handled by those services.</p><p className="mt-10 text-sm text-slate-500">Questions: <a className="text-green-700" href="mailto:support@navgrove.com">support@navgrove.com</a></p></article>; }
